# Models Directory

Trained model artifacts are written here during local runs.

Model binaries are not committed to this public repository because they are generated artifacts and may indirectly encode information from the source data.

